<Page x:Class="Faster_than_Light.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Faster_than_Light.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800" MinHeight="600" MinWidth="800"
      Title="MainPage">

    <Grid Name="mainGrid2" >

        <Rectangle Fill="#046380"></Rectangle>
        <!---->
        <TabControl x:Name="MainStackPanel" TabStripPlacement="Left">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Grid Name="Panel">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Panel" Property="Background" Value="#033b4d" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Panel" Property="Background" Value="Transparent" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem HorizontalAlignment="Left" >
                <TabItem.Header>
                    <StackPanel  Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/DriverIdentification_white.png" MaxHeight="50" MinHeight="15"/>
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">ВУ</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="DriverIdentificationDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="DriverIdentification_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />

                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID ВУ"
                                    Binding="{Binding Path=GuideReferencesID}" x:Name="GuideReferencesIDView"/>
                            <DataGridTextColumn Header="Номер водительских прав"
                                    Binding="{Binding Path=DriverLicense}" x:Name="DriverLicenseView"/>
                            <DataGridCheckBoxColumn Header="B"
                                    Binding="{Binding Path=B}" x:Name="BView"/>
                            <DataGridCheckBoxColumn Header="BE"
                                    Binding="{Binding Path=BE}" x:Name="BEView"/>
                            <DataGridCheckBoxColumn Header="C"
                                    Binding="{Binding Path=C}" x:Name="CView"/>
                            <DataGridCheckBoxColumn Header="CE"
                                    Binding="{Binding Path=CE}" x:Name="CEView"/>
                            <DataGridTextColumn Header="Дата Получения"
                                    Binding="{Binding Path=DateReceipt}" x:Name="DateReceiptView"/>
                            <DataGridTextColumn Header="Дата Окончания"
                                    Binding="{Binding Path=TerminationDate}" x:Name="TerminationDateView"/>
                            <DataGridTextColumn Header="Сотрудник"
                                    Binding="{Binding Path=EmployeeID}" x:Name="EmploeeIDView"/>



                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>

            </TabItem>

            <TabItem HorizontalAlignment="Left" >
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal"  >
                        <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/CargoСategory_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Категории</TextBlock>
                    </StackPanel >
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="CargoСategoryDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="CargoСategoryDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->
                        <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Категории"
                                    Binding="{Binding Path=CategoryID}" x:Name="CategoryIDView"/>
                            <DataGridTextColumn Header="Наименование"
                                    Binding="{Binding Path=Name}" x:Name="NameView"/>
                            <DataGridTextColumn Header="Коэфициент"
                                    Binding="{Binding Path=TransportationCoefficient}" x:Name="TransportationCoefficientView"/>
                            <DataGridTextColumn Header="Комментарий"
                                    Binding="{Binding Path=Comments}" x:Name="CommentsView"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left" >
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/Client_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Клиенты</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel Margin="10">
                        <TextBlock FontSize="24">Клиенты</TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <Button Margin="0 10 10 10" Background="#046380" FontSize="" Padding="5" Foreground="White">Добавить</Button>
                            <Button Margin="10" Background="White" Padding="5">Изменить</Button>
                            <Button Margin="10" Background="White" Padding="5">Удалить</Button>
                        </StackPanel>
                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="ClientDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="ClientDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Клиента"
                                    Binding="{Binding Path=ClientID}" x:Name="ClientIDView"/>
                            <DataGridTextColumn Header="Имя"
                                    Binding="{Binding Path=Name}" x:Name="NameClientView"/>

                            <DataGridTextColumn Header="Фамилия"
                                    Binding="{Binding Path=Surname}" x:Name="SurnameClientView"/>
                            <DataGridTextColumn Header="Отчество"
                                    Binding="{Binding Path=Patronymic}" x:Name="PatronymicClientView"/>
                            <DataGridTextColumn Header="Дата Рождения"
                                    Binding="{Binding Path=Birthdate}" x:Name="BirthdateClientiew"/>
                            <DataGridTextColumn Header="Серия и номер паспорта"
                                    Binding="{Binding Path=SeriaNumberPassport}" x:Name="SeriaNumberPassportClientView"/>
                            <DataGridTextColumn Header="Номер телефона"
                                    Binding="{Binding Path=PhoneNumber}" x:Name="PhoneNumberClientView"/>
                            <DataGridTextColumn Header="Email"
                                    Binding="{Binding Path=Email}" x:Name="EmailClientView"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left" >
                <TabItem.Header >
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/Track_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Машины</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380"  Name="CarDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="CarDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />

                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Машины"
                                    Binding="{Binding Path=CarID}" x:Name="CarIDView"/>
                            <DataGridTextColumn Header="VIN"
                                    Binding="{Binding Path=VIN}" x:Name="VINView"/>
                            <DataGridTextColumn Header="Гос. Номер"
                                    Binding="{Binding Path=StateNumber}" x:Name="StateNumberView"/>
                            <DataGridTextColumn Header="Марка"
                                    Binding="{Binding Path=Stamp}" x:Name="StampView"/>
                            <DataGridTextColumn Header="Модель"
                                    Binding="{Binding Path=Model}" x:Name="ModelView"/>
                            <DataGridTextColumn Header="Пробег"
                                    Binding="{Binding Path=Mileage}" x:Name="MileageView"/>
                            <DataGridTextColumn Header="Следующее ТО"
                                    Binding="{Binding Path=NextMaintenance}" x:Name="NextMaintenanceView"/>
                            <DataGridTextColumn Header="Статус"
                                    Binding="{Binding Path=Status}" x:Name="StatusView"/>
                            <DataGridTextColumn Header="Место Базирования"
                                    Binding="{Binding Path=LocationBase}" x:Name="LocationBaseView"/>
                            <DataGridTextColumn Header="Назначеный Водитель"
                                    Binding="{Binding Path=DriverID}" x:Name="DriverIDView"/>


                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/LocationBase_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Места базирования</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="LocationBaseDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="LocationBaseDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />

                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Места базирования"
                                    Binding="{Binding Path=LocationBaseID}" x:Name="LocationBaseIDView"/>
                            <DataGridTextColumn Header="Адрес"
                                    Binding="{Binding Path=Address}" x:Name="AddressLocationView"/>
                            <DataGridTextColumn Header="Количсетво мест"
                                    Binding="{Binding Path=NumberSeats}" x:Name="NumberSeatsView"/>
                            <DataGridTextColumn Header="Регион"
                                    Binding="{Binding Path=Region}" x:Name="RegionView"/>
                            <DataGridTextColumn Header="Директор"
                                    Binding="{Binding Path=Director}" x:Name="DirectorView"/>
                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/Package_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Посылки</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="PackageDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="PackageDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Посылки"
                                    Binding="{Binding Path=PackageID}" x:Name="PackageIDiew"/>
                            <DataGridTextColumn Header="Клиент"
                                    Binding="{Binding Path=ClientID}" x:Name="ClientID"/>
                            <DataGridTextColumn Header="Комментарий"
                                    Binding="{Binding Path=Comments}" x:Name="Comments"/>
                            <DataGridTextColumn Header="Адрес отправки"
                                    Binding="{Binding Path=SendingAddress}" x:Name="SendingAddressView"/>
                            <DataGridTextColumn Header="Адрес доставки"
                                    Binding="{Binding Path=DeliveryAddress}" x:Name="DeliveryAddressView"/>
                            <DataGridTextColumn Header="Вес"
                                    Binding="{Binding Path=Weight}" x:Name="WeightView"/>
                            <DataGridTextColumn Header="Дата Принятия"
                                    Binding="{Binding Path=DateAcceptance}" x:Name="DateAcceptanceView"/>
                            <DataGridTextColumn Header="Дата доставки"
                                    Binding="{Binding Path=DateDeliveryToPoint}" x:Name="DateDeliveryToPointView"/>
                            <DataGridTextColumn Header="Дата выдачи"
                                    Binding="{Binding Path=DateIssue}" x:Name="DateIssueView"/>
                            <DataGridTextColumn Header="ID Сотрудника"
                                    Binding="{Binding Path=EmployeeID}" x:Name="EmployeeIDPackageView"/>
                            <DataGridTextColumn Header="Длина"
                                    Binding="{Binding Path=Length}" x:Name="LengthView"/>
                            <DataGridTextColumn Header="Ширина"
                                    Binding="{Binding Path=Width}" x:Name="WidthView"/>
                            <DataGridTextColumn Header="Высота"
                                    Binding="{Binding Path=Height}" x:Name="HeightView"/>
                            <DataGridTextColumn Header="Тип упаковки"
                                    Binding="{Binding Path=PackageType}" x:Name="PackageTypeView"/>
                            <DataGridTextColumn Header="Стоимость доставки"
                                    Binding="{Binding Path=DeliveryCost}" x:Name="DeliveryCostView"/>
                            <DataGridTextColumn Header="Категория"
                                    Binding="{Binding Path=CargoCategoryEntity.Name}" x:Name="CargoCategoryView"/>
                            <DataGridTextColumn Header="Статус"
                                    Binding="{Binding Path=Status}" x:Name="StatusPackageView"/>
                            <DataGridTextColumn Header="Машина"
                                    Binding="{Binding Path=CarID}" x:Name="CarIDPackageView"/>
                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/PointReception_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Пункты выдачи</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="PointReceptionDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="PointReceptionDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />

                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Пункта Выдачи"
                                    Binding="{Binding Path=PointID}" x:Name = "PointIDView"/>
                            <DataGridTextColumn Header="Адрес"
                                    Binding="{Binding Path=Address}" x:Name = "AddressPointView"/>
                            <DataGridTextColumn Header="Директор"
                                    Binding="{Binding Path=Director}" x:Name = "DirectorPointView"/>
                            <DataGridTextColumn Header="Склад"
                                    Binding="{Binding Path=WarehouseID}" x:Name = "WarehouseIDPointView"/>
                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/Warehouse_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Склады</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="WarehouseDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="WarehouseDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Склада"
                                    Binding="{Binding Path=WarehouseID}" x:Name = "WarehouseIDView"/>
                            <DataGridTextColumn Header="Адрес"
                                    Binding="{Binding Path=Address}" x:Name = "AddressWarehouseView"/>
                            <DataGridTextColumn Header="Регион"
                                    Binding="{Binding Path=Region}" x:Name = "RegionWarehouseView"/>
                            <DataGridTextColumn Header="Директор"
                                    Binding="{Binding Path=Director}" x:Name = "DirectorWarehouseView"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/Employee_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Сотрудники</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                <Grid HorizontalAlignment="Left" Background="White">
                    <StackPanel>

                    </StackPanel>
                    <DataGrid Margin="10 100 10 10 " HorizontalGridLinesBrush="#046380" VerticalGridLinesBrush="#046380"  Name="EmployeeDataGridView"   AutoGenerateColumns="False"
                  FontSize="16">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="MouseDoubleClick" Handler="EmployeeDataGridRow_MouseDoubleClick"/>
                            </Style>
                        </DataGrid.Resources>
                        <!--<DataGrid.ContextMenu>
                -->
                        <!--<ContextMenu>
                    <MenuItem Header="Подробнее" Click="MenuItem_Click"></MenuItem>
                    <MenuItem Header="Редактировать" Click="editButton_Click"></MenuItem>
                    <MenuItem Header="Удалить" Click="removeButton_Click"></MenuItem>
                </ContextMenu>-->
                        <!--
            </DataGrid.ContextMenu>-->

                        <DataGrid.Columns>

                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="editButton" Click="EditButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Height="20" Source="/Image/edit_blue.png" />
                                            </Button>
                                            <Button Height="25" Width="25"
                                        Background="Transparent"
                                        BorderThickness="0" Name="removeButton" Click="removeButton_Click">
                                                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/trashcan_blue.png" />

                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn  Header="ID Сотрудника"
                                    Binding="{Binding Path=EmployeeID}" x:Name="EmployeeIDView"/>
                            <DataGridTextColumn Header="Имя"
                                    Binding="{Binding Path=Name}" x:Name="NameEmployeeView"/>
                            <DataGridTextColumn Header="Фамилия"
                                    Binding="{Binding Path=Surname}" x:Name="SurnameEmployeeView"/>
                            <DataGridTextColumn Header="Отчество"
                                    Binding="{Binding Path=Patronymic}" x:Name="PatronymicEmployeeView"/>
                            <DataGridTextColumn Header="Дата Рождения"
                                    Binding="{Binding Path=Birthdate}" x:Name="BirthdateEmployeeiew"/>
                            <DataGridTextColumn Header="Серия и номер паспорта"
                                    Binding="{Binding Path=SeriaNumberPassport}" x:Name="SeriaNumberPassportEmployeeView"/>
                            <DataGridTextColumn Header="Номер телефона"
                                    Binding="{Binding Path=PhoneNumber}" x:Name="PhoneNumberEmployeeView"/>
                            <DataGridTextColumn Header="Адрес Проживания"
                                    Binding="{Binding Path=ResidentialAddress}" x:Name="ResidentialAddressEmployeeView"/>
                            <DataGridTextColumn Header="Email"
                                    Binding="{Binding Path=Email}" x:Name="EmailEmployeeView"/>
                            <DataGridTextColumn Header="Должность"
                                    Binding="{Binding Path=Post}" x:Name="PostEmployeeView"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem HorizontalAlignment="Left">
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Image RenderOptions.BitmapScalingMode="HighQuality"  Source="/Image/gear_with_key_white.png" MaxHeight="50" MinHeight="15" />
                        <TextBlock Margin="10" Foreground="White" FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Left">Администрирование</TextBlock>
                    </StackPanel>
                </TabItem.Header>
                ну емае
            </TabItem>
        </TabControl>
        <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left">
            <Button Margin="10 0 0 10" Background="Transparent" BorderBrush="Transparent" Name="Exit" Click="Exit_Click">
                <StackPanel Orientation="Horizontal">
                   
                    <Image MaxHeight="50" MinHeight="15" RenderOptions.BitmapScalingMode="HighQuality" Source="/Image/door_white.png" />
                    <TextBlock HorizontalAlignment="Center" Margin="10" Foreground="white" FontSize="18">Выход</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>

    </Grid>
</Page>